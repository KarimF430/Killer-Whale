# MongoDB Connection
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/motoroctane

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/motoroctane

# MongoDB Backup Database (for disaster recovery)
# Set BACKUP_SYNC_ENABLED=true to enable daily sync to backup database
BACKUP_SYNC_ENABLED=false
# MONGODB_BACKUP_URI=mongodb+srv://username:password@backup-cluster.mongodb.net/gadizone_backup

# JWT Secret
JWT_SECRET=motoroctane-super-secret-key-change-in-production

# Server Configuration
NODE_ENV=development
PORT=5001

# Cloudflare R2 Storage Configuration (Required for image uploads)
R2_BUCKET=your_bucket_name_here
R2_ACCOUNT_ID=your_account_id_here
R2_ACCESS_KEY_ID=your_r2_access_key_here
R2_SECRET_ACCESS_KEY=your_r2_secret_key_here
R2_PUBLIC_BASE_URL=https://your-public-base-url.r2.dev
R2_REGION=auto

# Sentry Error Tracking (Optional)
# SENTRY_DSN=https://your-key@sentry.io/your-project-id

# Redis Configuration
# Primary Redis (required for caching/sessions)
# REDIS_URL=redis://default:password@your-redis.upstash.io:6379

# Redis Failover (optional - for high availability)
# Set REDIS_FAILOVER_ENABLED=true to enable automatic backup switching
REDIS_FAILOVER_ENABLED=false
# REDIS_BACKUP_URL=redis://default:password@backup-redis.upstash.io:6379

# Instructions:
# 1. Copy this file to .env
# 2. Update MONGODB_URI with your MongoDB connection string
# 3. Change JWT_SECRET to a strong random string in production
# 4. Get R2 credentials from Cloudflare Dashboard → R2 → Manage R2 API tokens
# 5. Create token with "Object Read & Write" permissions
# 6. Update R2_ACCESS_KEY_ID and R2_SECRET_ACCESS_KEY with your actual credentials
